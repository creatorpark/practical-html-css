파싱 단계에서 초기화된 계산된 값은 뷰포트 기반 값을 반영하지 않습니다.
뷰포트 크기는 파싱 단계 이후에 결정되기 때문입니다.



성능 최적화: 
파싱 단계에서 모든 CSS 규칙을 완전히 해석하고 
계산하는 것은 성능적으로 비용이 많이 드는 작업입니다.

필요한 정보 부족: 
레이아웃 단계에서는 뷰포트 크기가 이미 결정되었으므로
vh와 vw 값을 정확하게 계산할 수 있습니다.